# Get a free key at https://propodds.com (100 req/day free tier)
# Lines are cached 15 min so the free tier goes a long way.
PROP_ODDS_API_KEY=your_key_here

CACHE_TTL_SECONDS=3600
MODEL_ARTIFACT_PATH=artifacts/strikeout_model.joblib
